import { supabase } from '../utils/supabaseClient'

export default function BuyWebsite({user}) {
    return (
      <div className="container" style={{ padding: '50px 0 100px 0' }}>
        <h1>HELOOOOOOOOO</h1>
      </div>
    )
}
